diff --git a/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/check_production_weights.py b/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/check_production_weights.py
new file mode 100644
index 0000000..2566a8b
--- /dev/null
+++ b/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/check_production_weights.py
@@ -0,0 +1,153 @@
+#!/usr/bin/env python3
+'''
+Read the output of a --test production and decide whether to launch the rest.
+
+    python3 covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/check_production_weights.py <out_dir>/rjpsi_chunk*.root
+    python3 covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/check_production_weights.py file.root --tree tree
+
+Checks, per channel (gen_bc_decay 1 = J/psi mu nu, 7 = J/psi tau nu):
+
+  1. hammer_status   : share of signal rows with a valid weight; declined (3) or
+                       non-finite (4) above a few per mille is a problem
+  2. hammer_weight   : finite, positive, mean printed. For reference, the MC-level
+                       mean Kiselev -> Harrison weight in the mu channel was ~0.5 in
+                       the standalone study (this is per selected CANDIDATE, so
+                       selection shifts it: a sanity scale, not a closure)
+  3. variations      : each of the 14 non-degenerate directions must move the weight;
+                       direction 14 (null for this card) must not. All variations
+                       equal to nominal = the eigenvector plumbing did not take.
+  4. non-signal rows : hammer_* must be NaN there
+  5. lifetime        : gen_bc_ctau_weight* present, finite, mean close to 1, and
+                       up/down on opposite sides of nominal
+
+Exit code 1 if any HARD check fails, so it can gate a submission script.
+'''
+import argparse
+import sys
+
+import numpy as np
+import uproot
+
+N_DIR = 15
+NULL_DIRS = {14}          # pinned by the regulariser for the current card
+SIGNAL = {1: 'mu', 7: 'tau'}
+
+
+def main():
+    ap = argparse.ArgumentParser(description=__doc__,
+                                 formatter_class=argparse.RawDescriptionHelpFormatter)
+    ap.add_argument('inputs', nargs='+')
+    ap.add_argument('--tree', default='tree')
+    args = ap.parse_args()
+
+    var = ['hammer_ff_ev%02d_%s' % (j, t) for j in range(N_DIR) for t in ('up', 'dn')]
+    need = (['gen_bc_decay', 'hammer_weight', 'hammer_status',
+             'gen_bc_ctau_weight', 'gen_bc_ctau_weight_up', 'gen_bc_ctau_weight_down']
+            + var)
+
+    with uproot.open('%s:%s' % (args.inputs[0], args.tree)) as t:
+        missing = [b for b in need if b not in t.keys()]
+    if missing:
+        sys.exit('[FAIL] branches missing from the ntuple: %s\n'
+                 '       (was the production run from a checkout with the Hammer '
+                 'and lifetime patches?)' % missing[:6])
+
+    arr = {}
+    for chunk in uproot.iterate(['%s:%s' % (f, args.tree) for f in args.inputs],
+                                expressions=need, library='np', step_size='200 MB'):
+        for k, v in chunk.items():
+            arr.setdefault(k, []).append(np.asarray(v, dtype=float))
+    arr = dict((k, np.concatenate(v)) for k, v in arr.items())
+    code = np.round(np.nan_to_num(arr['gen_bc_decay'], nan=-1)).astype(int)
+    print('rows: %d from %d file(s)' % (code.size, len(args.inputs)))
+
+    hard_fail = []
+
+    # ---- 1-3: signal channels -------------------------------------------------
+    for c, name in SIGNAL.items():
+        sel = code == c
+        n = int(sel.sum())
+        print('\n[%s] %d signal rows' % (name, n))
+        if not n:
+            print('    none in this sample')
+            continue
+        st = arr['hammer_status'][sel]
+        for s, lab in ((0, 'ok'), (2, 'gen leaves missing'), (3, 'declined by Hammer'),
+                       (4, 'non-finite')):
+            k = int(np.sum(st == s))
+            if k:
+                print('    status %d (%s): %d  (%.3f%%)' % (s, lab, k, 100. * k / n))
+        if np.all(np.isnan(st)):
+            hard_fail.append('%s: hammer_status all NaN -- Hammer did not run' % name)
+            continue
+        bad = np.sum((st == 3) | (st == 4)) / float(n)
+        if bad > 5e-3:
+            hard_fail.append('%s: %.2f%% of signal rows declined or non-finite'
+                             % (name, 100 * bad))
+
+        w = arr['hammer_weight'][sel & (arr['hammer_status'] == 0)]
+        w = w[np.isfinite(w)]
+        if w.size == 0:
+            hard_fail.append('%s: no finite nominal weight' % name)
+            continue
+        print('    hammer_weight: mean %.4f  median %.4f  min %.3g  max %.3g'
+              % (w.mean(), np.median(w), w.min(), w.max()))
+        if np.any(w <= 0):
+            hard_fail.append('%s: non-positive nominal weights' % name)
+
+        ok = sel & (arr['hammer_status'] == 0)
+        nom = arr['hammer_weight'][ok]
+        moved, frozen = [], []
+        for j in range(N_DIR):
+            up = arr['hammer_ff_ev%02d_up' % j][ok]
+            dn = arr['hammer_ff_ev%02d_dn' % j][ok]
+            shift = np.nanmean(np.abs(up - nom) / np.maximum(np.abs(nom), 1e-12))
+            asym = np.nanmean((up + dn) / 2. - nom) / max(np.nanmean(nom), 1e-12)
+            (moved if shift > 1e-9 else frozen).append(j)
+            if j < 3 or j in NULL_DIRS:
+                print('    ev%02d: <|up-nom|/nom> %.2e   <(up+dn)/2-nom>/<nom> %+.1e'
+                      % (j, shift, asym))
+        print('    directions that move the weight: %d of %d  (expected %d)'
+              % (len(moved), N_DIR, N_DIR - len(NULL_DIRS)))
+        should_move = [j for j in range(N_DIR) if j not in NULL_DIRS]
+        if not moved:
+            hard_fail.append('%s: NO variation moves the weight -- the eigenvector '
+                             'plumbing did not take' % name)
+        elif set(should_move) - set(moved):
+            hard_fail.append('%s: directions %s should move the weight and do not'
+                             % (name, sorted(set(should_move) - set(moved))))
+
+    # ---- 4: non-signal rows ----------------------------------------------------
+    other = ~np.isin(code, list(SIGNAL))
+    if other.any():
+        leak = np.isfinite(arr['hammer_weight'][other]).sum()
+        print('\nnon-signal rows: %d, with a finite hammer_weight: %d' % (other.sum(), leak))
+        if leak:
+            hard_fail.append('%d non-signal rows carry a Hammer weight' % leak)
+
+    # ---- 5: lifetime -------------------------------------------------------------
+    has_bc = np.isfinite(arr['gen_bc_ctau_weight'])
+    print('\nlifetime weights on %d rows with a gen Bc' % has_bc.sum())
+    if has_bc.any():
+        n0 = arr['gen_bc_ctau_weight'][has_bc]
+        nu = arr['gen_bc_ctau_weight_up'][has_bc]
+        nd = arr['gen_bc_ctau_weight_down'][has_bc]
+        print('    mean nominal %.4f  up %.4f  down %.4f' % (n0.mean(), nu.mean(), nd.mean()))
+        if not (0.95 < n0.mean() < 1.05):
+            hard_fail.append('ctau nominal weight mean %.3f far from 1' % n0.mean())
+        if not ((nu.mean() - n0.mean()) * (nd.mean() - n0.mean()) < 0):
+            print('    [WARN] up and down are not on opposite sides of nominal on '
+                  'average (can happen after selection; check the lifetime shape)')
+    else:
+        hard_fail.append('no row carries a lifetime weight')
+
+    print()
+    if hard_fail:
+        for msg in hard_fail:
+            print('[FAIL] ' + msg)
+        sys.exit(1)
+    print('[OK] all hard checks passed -- safe to launch the full production')
+
+
+if __name__ == '__main__':
+    main()
diff --git a/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_bc_mc_pnfs_21sep26.py b/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_bc_mc_pnfs_21sep26.py
new file mode 100644
index 0000000..41f7c6a
--- /dev/null
+++ b/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_bc_mc_pnfs_21sep26.py
@@ -0,0 +1,385 @@
+'''
+Submitter for the SLURM system (PSI Tier-3) -- Bc signal MC, production of 21 Sep 2026.
+
+Adds, with respect to RJpsi_17Sep2026_notrig_BcToJPsiMuMu_inclusive_covflow_v1:
+
+  * Bc lifetime weights       gen_bc_ctau_weight{,_up,_down}   (always on for MC)
+  * Hammer FF weights         hammer_weight, hammer_ff_ev00..14_{up,dn},
+                              hammer_status: Kiselev -> Harrison-2024, central +
+                              the 2 x 15 eigenvariations of the validated card
+and keeps
+  * track covariance fix      --covflow with the state-of-the-art model,
+                              covflow_mu2_1Mevents_fourconditional_block_17sep26_v4
+                              (context log_pt, eta, n_pix_b_hit, n_pix_e_hit;
+                              block features; logsigma_corr)
+
+Run from anywhere in a cmsenv'd shell (output lands in the CURRENT directory, as
+before); conventionally from Bmmm/Analysis/test/rjpsi:
+
+    python3 covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_bc_mc_pnfs_21sep26.py --test 2
+    python3 covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/check_production_weights.py <out_dir>_test/*.root
+    python3 covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_bc_mc_pnfs_21sep26.py
+
+Paths are anchored on $CMSSW_BASE, not on the working directory: the entry point,
+the file list and the covflow model all come from the release the jobs run.
+
+Two things are SNAPSHOTTED into the output directory and the jobs read the copies:
+the FF card and the covflow model. Refitting either in the release later cannot
+change a production that is already running, and the inputs that made the
+ntuples are kept next to them. PROVENANCE.json records the commit and hashes.
+
+Everything that would make EVERY job fail is checked here, at submission time:
+the FF card (P1 convention, validated covariance), the Hammer install, the covflow
+model -- which is actually LOADED once, exactly as each job will load it -- and the
+entry point.
+
+--test N first: the inline Hammer path with BGLVar and 30 eigenvariations has
+never run at scale, so job time and memory are unknown.
+'''
+
+import argparse
+import datetime
+import hashlib
+import json
+import os
+import shutil
+import subprocess
+from glob import glob
+
+ap = argparse.ArgumentParser(description=__doc__,
+                             formatter_class=argparse.RawDescriptionHelpFormatter)
+ap.add_argument('--test', type=int, default=0, metavar='N',
+                help='submit only the first N chunks, into <out_dir>_test')
+ap.add_argument('--dry-run', action='store_true',
+                help='write the job scripts and the provenance, submit nothing')
+args = ap.parse_args()
+
+resubmit = False
+toresubmit = []          # chunk indices, used only when resubmit = True
+
+old_files = []
+files = []
+
+# NEW: everything read from the release, not from the working directory
+cmssw_base = os.environ.get('CMSSW_BASE', '')
+if not cmssw_base:
+    raise RuntimeError('CMSSW_BASE is not set -- run `cmsenv` in your CMSSW_X_Y_Z/src before launching this submitter.')
+RJPSI_DIR  = os.path.join(cmssw_base, 'src', 'Bmmm', 'Analysis', 'test', 'rjpsi')
+INPUT_LIST = os.path.join(RJPSI_DIR, 'files_bc_skim_15jun26.txt')
+
+with open(INPUT_LIST) as f:
+    ifiles = f.read().splitlines()
+    ifiles = ['root://t3dcachedb03.psi.ch:1094//'+ifile for ifile in ifiles if ifile not in old_files]
+    files += ifiles
+
+files_per_job = 1
+chunks = list(map(list, list(zip(*[iter(files)]*files_per_job))))
+
+if len(files)%files_per_job!=0:
+    last_idx = len(files)%files_per_job
+    chunks += [files[-last_idx:]]
+
+queue = 'standard'; time = 720
+# queue = 'short'   ; time = 60
+# queue = 'long'    ; time = 10080
+
+# out_dir = 'RJpsi_17Sep2026_notrig_BcToJPsiMuMu_inclusive_covflow_v1'
+out_dir = 'RJpsi_21Sep2026_notrig_BcToJPsiMuMu_inclusive_covflowv4_hammer_ctau_v1'
+if args.test:
+    out_dir += '_test'
+
+out_file_name = 'rjpsi'
+
+cfg = 'inspector_rjpsi.py'
+CFG_SRC = os.path.join(RJPSI_DIR, cfg)
+
+# ------------------------------------------------------------------------------------------
+# NEW (21 Sep 2026): Hammer and the FF card
+# ------------------------------------------------------------------------------------------
+HAMMER_PREFIX = '/work/manzoni/hammer'
+HAMMER_ENV    = HAMMER_PREFIX + '/hammer_env.sh'   # generated by install_hammer.sh (bash)
+CARD_NAME     = 'harrison_bglvar.json'
+# NEW: the state-of-the-art covflow model (in the repository since 8bd92d7)
+COVFLOW_RUN   = 'covflow_mu2_1Mevents_fourconditional_block_17sep26_v4'
+COVFLOW_MODEL = os.path.join(RJPSI_DIR, COVFLOW_RUN,
+                             'mu2_1Mevents_fourconditional_block_17sep26_v4', 'task_0')
+
+# CMSSW release to set up inside the job, captured from the current shell.
+scram_arch = os.environ.get('SCRAM_ARCH', '')
+
+##########################################################################################
+# PREFLIGHT: anything that would fail every job is caught here, once
+##########################################################################################
+
+def die(msg):
+    raise SystemExit('[PREFLIGHT] ' + msg)
+
+if not os.path.isfile(CFG_SRC):
+    die('entry point %s not found.' % CFG_SRC)
+
+# the covflow model: the same REQUIRED_FILES the corrector checks
+from Bmmm.Analysis.utils import CovFlowCorrector, make_cov_corrector
+if not os.path.isdir(COVFLOW_MODEL):
+    die('covflow model directory %s not found.' % COVFLOW_MODEL)
+cf_missing = [f for f in CovFlowCorrector.REQUIRED_FILES
+              if not os.path.isfile(os.path.join(COVFLOW_MODEL, f))]
+if cf_missing:
+    die('covflow model %s is missing %s.' % (COVFLOW_MODEL, cf_missing))
+# LOAD it once, exactly as every job will, before anything is created on disk: a
+# model that cannot be loaded -- wrong hyperparameters in covflow.json, missing
+# covflow package, torch mismatch -- then fails here once, and leaves no empty
+# output directories behind, instead of failing in every job.
+try:
+    _corrector = make_cov_corrector(COVFLOW_MODEL)
+except Exception as exc:                                     # noqa: BLE001
+    die('the covflow model %s does not load: %s: %s'
+        % (COVFLOW_MODEL, type(exc).__name__, exc))
+print('#### covflow model loads: context %s' % (_corrector.context_names,))
+if not os.path.isfile(HAMMER_ENV):
+    die('%s not found: Hammer is not installed where the jobs expect it '
+        '(run install_hammer.sh, or fix HAMMER_PREFIX).' % HAMMER_ENV)
+
+# The card is validated with the SAME code the jobs will use: HammerFF.load_card
+# checks unitarity and shapes, and P1_CONVENTION is the single source of truth for
+# the convention a card must be fitted in. Importing HammerFF does not import
+# hammer itself, so this works in any cmsenv'd shell.
+from Bmmm.Analysis.HammerFF import P1_CONVENTION, load_card
+card_src = os.path.join(cmssw_base, 'src', 'Bmmm', 'Analysis', 'data', CARD_NAME)
+if not os.path.isfile(card_src):
+    die('FF card %s not found.' % card_src)
+card = load_card(card_src)
+if card.get('p1_convention') != P1_CONVENTION:
+    die('FF card %s is not fitted in the P1 convention Hammer uses (%r, card says %r). '
+        'Its tau weights would be biased by ~+1%% on R. Refit it.'
+        % (card_src, P1_CONVENTION, card.get('p1_convention')))
+if str(card.get('covariance_status', '')).lower() != 'validated':
+    die('FF card %s has covariance_status=%r: the jobs would refuse to write the '
+        'eigenvariations. Validate the covariance and set it to "validated".'
+        % (card_src, card.get('covariance_status')))
+
+##########################################################################################
+##########################################################################################
+
+# make output dir
+if not os.path.exists(out_dir):
+    try:
+        os.makedirs('/pnfs/psi.ch/cms/trivcat/store/user/manzoni/'+out_dir)
+    except:
+        print('pnfs directory exists')
+    os.makedirs(out_dir)
+    os.makedirs(out_dir + '/logs')
+    os.makedirs(out_dir + '/errs')
+
+shutil.copy2(CFG_SRC, out_dir)
+
+# NEW: snapshot the FF card into the production directory. The jobs read THIS
+# copy, so editing or refitting the card in the release later cannot change a
+# production that is already running, and the card that made the weights is
+# kept next to them.
+card_used = os.path.join(os.getcwd(), out_dir, CARD_NAME)
+shutil.copy2(card_src, card_used)
+
+# NEW: snapshot the covflow model (the four files the corrector needs, ~2 MB).
+# The jobs read the snapshot; it is checked byte for byte against the model that
+# was just loaded in the preflight, so what the jobs use is what was tested.
+def _sha(path):
+    with open(path, 'rb') as fh:
+        return hashlib.sha256(fh.read()).hexdigest()
+
+covflow_used = os.path.join(os.getcwd(), out_dir, 'covflow_model')
+os.makedirs(covflow_used, exist_ok=True)
+cf_hashes = {}
+for fname in CovFlowCorrector.REQUIRED_FILES:
+    src = os.path.join(COVFLOW_MODEL, fname)
+    shutil.copy2(src, covflow_used)
+    cf_hashes[fname] = _sha(os.path.join(covflow_used, fname))
+    if cf_hashes[fname] != _sha(src):
+        die('snapshot of %s differs from its source' % fname)
+with open(os.path.join(covflow_used, 'covflow.json')) as fcfg:
+    covflow_cfg = json.load(fcfg)
+
+# NEW: provenance, written at submission time
+def sh(cmd):
+    try:
+        return subprocess.check_output(cmd, shell=True, stderr=subprocess.STDOUT,
+                                       universal_newlines=True).strip()
+    except subprocess.CalledProcessError as exc:
+        return '<failed: %s>' % exc.output.strip()
+
+repo = os.path.join(cmssw_base, 'src', 'Bmmm')
+dirty = sh('git -C %s status --porcelain --untracked-files=no' % repo)
+with open(card_used, 'rb') as fcard:
+    card_sha = hashlib.sha256(fcard.read()).hexdigest()
+provenance = {
+    'submitted'        : datetime.datetime.now().isoformat(timespec='seconds'),
+    'out_dir'          : out_dir,
+    'cmssw_base'       : cmssw_base,
+    'scram_arch'       : scram_arch,
+    'bmmm_commit'      : sh('git -C %s rev-parse HEAD' % repo),
+    'bmmm_dirty_files' : dirty.splitlines() if dirty else [],
+    'n_chunks'         : len(chunks),
+    'test'             : args.test,
+    'input_list'       : INPUT_LIST,
+    'covflow'          : {'source': COVFLOW_MODEL, 'snapshot': covflow_used,
+                          'config': covflow_cfg, 'sha256': cf_hashes},
+    'hammer_env'       : HAMMER_ENV,
+    'ff_card'          : {'file': card_used, 'sha256': card_sha,
+                          'name': card.get('name'), 'fit_date': card.get('fit_date'),
+                          'p1_convention': card.get('p1_convention'),
+                          'covariance_status': card.get('covariance_status')},
+    'ctau_weights'     : 'tau_MC 0.507 ps -> PDG 0.510 +/- 0.010 ps (gen_bc_ctau_weight*)',
+}
+with open(os.path.join(out_dir, 'PROVENANCE.json'), 'w') as fprov:
+    json.dump(provenance, fprov, indent=1)
+if dirty:
+    print('[WARN] the Bmmm checkout has uncommitted changes; they are listed in '
+          '%s/PROVENANCE.json' % out_dir)
+print('#### provenance: Bmmm %s, card %s (sha256 %s...)'
+      % (provenance['bmmm_commit'][:10], card.get('fit_date'), card_sha[:12]))
+
+n_submitted = 0
+for ijob, ichunk in enumerate(chunks):
+
+    if resubmit:
+        if ijob not in toresubmit: continue
+
+    if args.test and ijob >= args.test:
+        break
+
+    to_write = '\n'.join([
+        '#!/bin/bash',
+        '',
+        '# --- scratch dir ---',
+        'mkdir -p /scratch/manzoni/{scratch_dir}',
+        'ls /scratch/manzoni/',
+        'FAILED_PARTS=0',
+        '',
+        '# --- CMSSW runtime (native, no container) ---',
+        'export SCRAM_ARCH={scram_arch}',
+        'source /cvmfs/cms.cern.ch/cmsset_default.sh',
+        'echo ">>>> moving to {dir}"',
+        'cd {cmssw_base}/src',
+        'echo ">>>> now in $PWD"',
+        'eval `scramv1 runtime -sh`',
+        'echo ">>>> CMSSW_BASE=$CMSSW_BASE"',
+        'echo ">>>> using python: $(which python3)"',
+        '',
+        '# --- fail loudly if cmsenv did not take effect ---',
+        'python3 -c "import sys; print(\'>>>> python startup OK\', sys.version.split()[0])"',
+        'if [ $? -ne 0 ]; then',
+        '    echo ">>>> FATAL: cmsenv did not take effect (CMSSW python cannot find its standard library). Aborting chunk {ijob}."',
+        '    exit 1',
+        'fi',
+        'which hadd',
+        '',
+        '# --- NEW: Hammer on the path, after cmsenv (it must not be overridden) ---',
+        'source {hammer_env}',
+        'python3 -c "import hammer; print(\'>>>> hammer\', getattr(hammer, \'version\', \'?\'), hammer.__file__)"',
+        'if [ $? -ne 0 ]; then',
+        '    echo ">>>> FATAL: hammer is not importable on this node. Aborting chunk {ijob}."',
+        '    exit 1',
+        'fi',
+        '',
+        '# --- grid proxy: stage a private copy that survives the whole job ---',
+        'cp $X509_USER_PROXY /scratch/manzoni/{scratch_dir}/x509proxy_{ijob}',
+        'chmod 600 /scratch/manzoni/{scratch_dir}/x509proxy_{ijob}',
+        'export X509_USER_PROXY=/scratch/manzoni/{scratch_dir}/x509proxy_{ijob}',
+        '',
+        '# --- run from the output dir so per-job loggers land there ---',
+        'cd {dir}',
+        'echo ">>>> now running in $PWD"',
+        '',
+    ]).format(
+        dir         = '/'.join([os.getcwd(), out_dir]),
+        scratch_dir = out_dir,
+        cmssw_base  = cmssw_base,
+        scram_arch  = scram_arch,
+        hammer_env  = HAMMER_ENV,
+        ijob        = ijob,
+    )
+
+    for idx, ifile in enumerate(ichunk):
+        to_write += (
+            'python3 {dir}/{cfg} '
+            '--inputFiles={infiles} '
+            '--logfreq=5000 '
+            '--destination=/scratch/manzoni/{scratch_dir} '
+            '--savenontrig '
+            '--covflow {covflow} '
+            '--hammer {card} '
+            '--mc '
+            '--skim '
+            '--filename={outfile}_chunk{ijob}_part{idx} \n'
+            'if [ $? -ne 0 ]; then\n'
+            '    echo ">>>> FAILED: part{idx} of chunk{ijob} ({infiles})"\n'
+            '    FAILED_PARTS=$((FAILED_PARTS+1))\n'
+            'fi\n'
+        ).format(
+            dir         = '/'.join([os.getcwd(), out_dir]),
+            covflow     = covflow_used,
+            card        = card_used,
+            scratch_dir = out_dir,
+            cfg         = cfg,
+            outfile     = out_file_name,
+            ijob        = ijob,
+            infiles     = ifile,
+            idx         = idx,
+        )
+
+    to_write += '\n'.join([
+        '',
+        'ls -latrh /scratch/manzoni/{scratch_dir}',
+        'echo ">>>> $FAILED_PARTS part(s) failed for chunk {ijob}"',
+        '',
+        'if [ $FAILED_PARTS -gt 0 ]; then',
+        '    echo ">>>> ABORTING merge and transfer for chunk {ijob}: not all parts succeeded"',
+        '    exit 1',
+        'fi',
+        '',
+        'hadd -f -k '
+        '/scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}.root '
+        '/scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}_part*.root',
+        '',
+        'xrdcp '
+        '/scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}.root '
+        'root://t3dcachedb03.psi.ch:1094///pnfs/psi.ch/cms/trivcat/store/user/manzoni/{se_dir}/{outfile}_chunk{ijob}.root',
+        '',
+        'if [ $? -eq 0 ]; then',
+        '    echo ">>>> xrdcp succeeded, cleaning scratch"',
+        '    rm -f /scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}.root',
+        '    rm -f /scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}_part*.root',
+        'else',
+        '    echo ">>>> xrdcp FAILED for chunk {ijob}, scratch files kept for inspection"',
+        '    exit 1',
+        'fi',
+        '',
+    ]).format(
+        scratch_dir = out_dir,
+        outfile     = out_file_name,
+        ijob        = ijob,
+        se_dir      = out_dir,
+    )
+
+    with open("%s/submitter_chunk%d.sh" %(out_dir, ijob), "wt") as flauncher:
+        flauncher.write(to_write)
+
+    command_sh_batch = ' '.join([
+        'sbatch',
+        '-p %s'%queue,
+        '--account=t3',
+        '-o %s/logs/chunk%d.log' %(out_dir, ijob),
+        '-e %s/errs/chunk%d.err' %(out_dir, ijob),
+        '--job-name=%d_%s' %(ijob, out_dir),
+        '--time=%d'%time,
+        '--nodes=1 --ntasks=1 --nodelist=t3wn[80-91]',
+        '--mem=4000',
+        '%s/submitter_chunk%d.sh' %(out_dir, ijob),
+    ])
+
+    print(command_sh_batch)
+    if not args.dry_run:
+        os.system(command_sh_batch)
+    n_submitted += 1
+
+print('#### %s %d chunk(s) of %d into %s'
+      % ('wrote (dry run)' if args.dry_run else 'submitted', n_submitted, len(chunks), out_dir))
diff --git a/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_hb_mc_pnfs_21sep26.py b/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_hb_mc_pnfs_21sep26.py
new file mode 100644
index 0000000..178347d
--- /dev/null
+++ b/Bmmm/Analysis/test/rjpsi/covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_hb_mc_pnfs_21sep26.py
@@ -0,0 +1,329 @@
+'''
+Submitter for the SLURM system (PSI Tier-3) -- Hb inclusive MC, production of 21 Sep 2026.
+
+Companion of submitter_bc_mc_pnfs_21sep26.py.
+
+  * track covariance fix      --covflow with the state-of-the-art model,
+                              covflow_mu2_1Mevents_fourconditional_block_17sep26_v4.
+                              NOTE: switched ON here. The Hb submitter shipped with
+                              this model has --covflow commented out, while the Bc
+                              one has it; Bc and Hb enter the same fit, so they
+                              should share the track covariance treatment.
+  * Hammer FF weights         NOT run. Hammer is validated against EvtGen BC_VMN 1
+                              as configured in OUR Bc DEC file. A Bc inside the Hb
+                              sample decays through EvtGen's generic table, whose
+                              model has not been checked. The hammer_* branches are
+                              in the schema anyway, all NaN (status NaN = not run).
+  * Bc lifetime weights       gen_bc_ctau_weight* are filled for any Bc, but assume
+                              tau_MC = 0.507 ps, the lifetime of the dedicated Bc
+                              sample. A Bc in the Hb sample gets its lifetime from
+                              the generic evt.pdl: DO NOT use these branches on this
+                              sample without checking that value.
+
+Run from anywhere in a cmsenv'd shell (output lands in the CURRENT directory):
+
+    python3 covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_hb_mc_pnfs_21sep26.py --test 2
+    python3 covflow_mu2_1Mevents_fourconditional_block_17sep26_v4/submitter_hb_mc_pnfs_21sep26.py
+
+The covflow model is snapshotted into the output directory and loaded once here,
+exactly as each job will load it.
+'''
+
+import argparse
+import datetime
+import hashlib
+import json
+import os
+import shutil
+import subprocess
+from glob import glob
+
+ap = argparse.ArgumentParser(description=__doc__,
+                             formatter_class=argparse.RawDescriptionHelpFormatter)
+ap.add_argument('--test', type=int, default=0, metavar='N',
+                help='submit only the first N chunks, into <out_dir>_test')
+ap.add_argument('--dry-run', action='store_true',
+                help='write the job scripts and the provenance, submit nothing')
+args = ap.parse_args()
+
+resubmit = False
+toresubmit = []          # chunk indices, used only when resubmit = True
+
+old_files = []
+files = []
+
+# NEW: everything read from the release, not from the working directory
+cmssw_base = os.environ.get('CMSSW_BASE', '')
+if not cmssw_base:
+    raise RuntimeError('CMSSW_BASE is not set -- run `cmsenv` in your CMSSW_X_Y_Z/src before launching this submitter.')
+RJPSI_DIR  = os.path.join(cmssw_base, 'src', 'Bmmm', 'Analysis', 'test', 'rjpsi')
+INPUT_LIST = os.path.join(RJPSI_DIR, 'files_hb_skim_15jun26.txt')
+
+with open(INPUT_LIST) as f:
+    ifiles = f.read().splitlines()
+    ifiles = ['root://t3dcachedb03.psi.ch:1094//'+ifile for ifile in ifiles if ifile not in old_files]
+    files += ifiles
+
+files_per_job = 1
+chunks = list(map(list, list(zip(*[iter(files)]*files_per_job))))
+
+if len(files)%files_per_job!=0:
+    last_idx = len(files)%files_per_job
+    chunks += [files[-last_idx:]]
+
+queue = 'standard'; time = 720
+# queue = 'short'   ; time = 60
+# queue = 'long'    ; time = 10080
+
+# out_dir = 'RJpsi_15Sep2026_notrig_HbToJPsiMuMu_inclusive_v1'
+out_dir = 'RJpsi_21Sep2026_notrig_HbToJPsiMuMu_inclusive_covflowv4_v1'
+if args.test:
+    out_dir += '_test'
+
+out_file_name = 'rjpsi'
+
+cfg = 'inspector_rjpsi.py'
+CFG_SRC = os.path.join(RJPSI_DIR, cfg)
+
+# ------------------------------------------------------------------------------------------
+# NEW (21 Sep 2026): Hammer and the FF card
+# ------------------------------------------------------------------------------------------
+# NEW: the state-of-the-art covflow model (in the repository since 8bd92d7)
+COVFLOW_RUN   = 'covflow_mu2_1Mevents_fourconditional_block_17sep26_v4'
+COVFLOW_MODEL = os.path.join(RJPSI_DIR, COVFLOW_RUN,
+                             'mu2_1Mevents_fourconditional_block_17sep26_v4', 'task_0')
+
+# CMSSW release to set up inside the job, captured from the current shell.
+scram_arch = os.environ.get('SCRAM_ARCH', '')
+
+##########################################################################################
+# PREFLIGHT: anything that would fail every job is caught here, once
+##########################################################################################
+
+def die(msg):
+    raise SystemExit('[PREFLIGHT] ' + msg)
+
+if not os.path.isfile(CFG_SRC):
+    die('entry point %s not found.' % CFG_SRC)
+
+# the covflow model: the same REQUIRED_FILES the corrector checks
+from Bmmm.Analysis.utils import CovFlowCorrector, make_cov_corrector
+if not os.path.isdir(COVFLOW_MODEL):
+    die('covflow model directory %s not found.' % COVFLOW_MODEL)
+cf_missing = [f for f in CovFlowCorrector.REQUIRED_FILES
+              if not os.path.isfile(os.path.join(COVFLOW_MODEL, f))]
+if cf_missing:
+    die('covflow model %s is missing %s.' % (COVFLOW_MODEL, cf_missing))
+# LOAD it once, exactly as every job will, before anything is created on disk: a
+# model that cannot be loaded -- wrong hyperparameters in covflow.json, missing
+# covflow package, torch mismatch -- then fails here once, and leaves no empty
+# output directories behind, instead of failing in every job.
+try:
+    _corrector = make_cov_corrector(COVFLOW_MODEL)
+except Exception as exc:                                     # noqa: BLE001
+    die('the covflow model %s does not load: %s: %s'
+        % (COVFLOW_MODEL, type(exc).__name__, exc))
+print('#### covflow model loads: context %s' % (_corrector.context_names,))
+
+##########################################################################################
+##########################################################################################
+
+# make output dir
+if not os.path.exists(out_dir):
+    try:
+        os.makedirs('/pnfs/psi.ch/cms/trivcat/store/user/manzoni/'+out_dir)
+    except:
+        print('pnfs directory exists')
+    os.makedirs(out_dir)
+    os.makedirs(out_dir + '/logs')
+    os.makedirs(out_dir + '/errs')
+
+shutil.copy2(CFG_SRC, out_dir)
+
+# NEW: snapshot the covflow model (the four files the corrector needs, ~2 MB).
+# The jobs read the snapshot; it is checked byte for byte against the model that
+# was just loaded in the preflight, so what the jobs use is what was tested.
+def _sha(path):
+    with open(path, 'rb') as fh:
+        return hashlib.sha256(fh.read()).hexdigest()
+
+covflow_used = os.path.join(os.getcwd(), out_dir, 'covflow_model')
+os.makedirs(covflow_used, exist_ok=True)
+cf_hashes = {}
+for fname in CovFlowCorrector.REQUIRED_FILES:
+    src = os.path.join(COVFLOW_MODEL, fname)
+    shutil.copy2(src, covflow_used)
+    cf_hashes[fname] = _sha(os.path.join(covflow_used, fname))
+    if cf_hashes[fname] != _sha(src):
+        die('snapshot of %s differs from its source' % fname)
+with open(os.path.join(covflow_used, 'covflow.json')) as fcfg:
+    covflow_cfg = json.load(fcfg)
+
+# NEW: provenance, written at submission time
+def sh(cmd):
+    try:
+        return subprocess.check_output(cmd, shell=True, stderr=subprocess.STDOUT,
+                                       universal_newlines=True).strip()
+    except subprocess.CalledProcessError as exc:
+        return '<failed: %s>' % exc.output.strip()
+
+repo = os.path.join(cmssw_base, 'src', 'Bmmm')
+dirty = sh('git -C %s status --porcelain --untracked-files=no' % repo)
+provenance = {
+    'submitted'        : datetime.datetime.now().isoformat(timespec='seconds'),
+    'out_dir'          : out_dir,
+    'cmssw_base'       : cmssw_base,
+    'scram_arch'       : scram_arch,
+    'bmmm_commit'      : sh('git -C %s rev-parse HEAD' % repo),
+    'bmmm_dirty_files' : dirty.splitlines() if dirty else [],
+    'n_chunks'         : len(chunks),
+    'test'             : args.test,
+    'input_list'       : INPUT_LIST,
+    'covflow'          : {'source': COVFLOW_MODEL, 'snapshot': covflow_used,
+                          'config': covflow_cfg, 'sha256': cf_hashes},
+    'hammer'           : 'not run on this sample (see docstring)',
+    'ctau_weights'     : 'filled, but assume tau_MC = 0.507 ps: NOT valid for Bc in '
+                         'this sample without checking the generic evt.pdl lifetime',
+}
+with open(os.path.join(out_dir, 'PROVENANCE.json'), 'w') as fprov:
+    json.dump(provenance, fprov, indent=1)
+if dirty:
+    print('[WARN] the Bmmm checkout has uncommitted changes; they are listed in '
+          '%s/PROVENANCE.json' % out_dir)
+print('#### provenance: Bmmm %s' % provenance['bmmm_commit'][:10])
+
+n_submitted = 0
+for ijob, ichunk in enumerate(chunks):
+
+    if resubmit:
+        if ijob not in toresubmit: continue
+
+    if args.test and ijob >= args.test:
+        break
+
+    to_write = '\n'.join([
+        '#!/bin/bash',
+        '',
+        '# --- scratch dir ---',
+        'mkdir -p /scratch/manzoni/{scratch_dir}',
+        'ls /scratch/manzoni/',
+        'FAILED_PARTS=0',
+        '',
+        '# --- CMSSW runtime (native, no container) ---',
+        'export SCRAM_ARCH={scram_arch}',
+        'source /cvmfs/cms.cern.ch/cmsset_default.sh',
+        'echo ">>>> moving to {dir}"',
+        'cd {cmssw_base}/src',
+        'echo ">>>> now in $PWD"',
+        'eval `scramv1 runtime -sh`',
+        'echo ">>>> CMSSW_BASE=$CMSSW_BASE"',
+        'echo ">>>> using python: $(which python3)"',
+        '',
+        '# --- fail loudly if cmsenv did not take effect ---',
+        'python3 -c "import sys; print(\'>>>> python startup OK\', sys.version.split()[0])"',
+        'if [ $? -ne 0 ]; then',
+        '    echo ">>>> FATAL: cmsenv did not take effect (CMSSW python cannot find its standard library). Aborting chunk {ijob}."',
+        '    exit 1',
+        'fi',
+        'which hadd',
+        '',
+        '# --- grid proxy: stage a private copy that survives the whole job ---',
+        'cp $X509_USER_PROXY /scratch/manzoni/{scratch_dir}/x509proxy_{ijob}',
+        'chmod 600 /scratch/manzoni/{scratch_dir}/x509proxy_{ijob}',
+        'export X509_USER_PROXY=/scratch/manzoni/{scratch_dir}/x509proxy_{ijob}',
+        '',
+        '# --- run from the output dir so per-job loggers land there ---',
+        'cd {dir}',
+        'echo ">>>> now running in $PWD"',
+        '',
+    ]).format(
+        dir         = '/'.join([os.getcwd(), out_dir]),
+        scratch_dir = out_dir,
+        cmssw_base  = cmssw_base,
+        scram_arch  = scram_arch,
+        ijob        = ijob,
+    )
+
+    for idx, ifile in enumerate(ichunk):
+        to_write += (
+            'python3 {dir}/{cfg} '
+            '--inputFiles={infiles} '
+            '--logfreq=5000 '
+            '--destination=/scratch/manzoni/{scratch_dir} '
+            '--savenontrig '
+            '--covflow {covflow} '
+            '--mc '
+            '--skim '
+            '--filename={outfile}_chunk{ijob}_part{idx} \n'
+            'if [ $? -ne 0 ]; then\n'
+            '    echo ">>>> FAILED: part{idx} of chunk{ijob} ({infiles})"\n'
+            '    FAILED_PARTS=$((FAILED_PARTS+1))\n'
+            'fi\n'
+        ).format(
+            dir         = '/'.join([os.getcwd(), out_dir]),
+            covflow     = covflow_used,
+            scratch_dir = out_dir,
+            cfg         = cfg,
+            outfile     = out_file_name,
+            ijob        = ijob,
+            infiles     = ifile,
+            idx         = idx,
+        )
+
+    to_write += '\n'.join([
+        '',
+        'ls -latrh /scratch/manzoni/{scratch_dir}',
+        'echo ">>>> $FAILED_PARTS part(s) failed for chunk {ijob}"',
+        '',
+        'if [ $FAILED_PARTS -gt 0 ]; then',
+        '    echo ">>>> ABORTING merge and transfer for chunk {ijob}: not all parts succeeded"',
+        '    exit 1',
+        'fi',
+        '',
+        'hadd -f -k '
+        '/scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}.root '
+        '/scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}_part*.root',
+        '',
+        'xrdcp '
+        '/scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}.root '
+        'root://t3dcachedb03.psi.ch:1094///pnfs/psi.ch/cms/trivcat/store/user/manzoni/{se_dir}/{outfile}_chunk{ijob}.root',
+        '',
+        'if [ $? -eq 0 ]; then',
+        '    echo ">>>> xrdcp succeeded, cleaning scratch"',
+        '    rm -f /scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}.root',
+        '    rm -f /scratch/manzoni/{scratch_dir}/{outfile}_chunk{ijob}_part*.root',
+        'else',
+        '    echo ">>>> xrdcp FAILED for chunk {ijob}, scratch files kept for inspection"',
+        '    exit 1',
+        'fi',
+        '',
+    ]).format(
+        scratch_dir = out_dir,
+        outfile     = out_file_name,
+        ijob        = ijob,
+        se_dir      = out_dir,
+    )
+
+    with open("%s/submitter_chunk%d.sh" %(out_dir, ijob), "wt") as flauncher:
+        flauncher.write(to_write)
+
+    command_sh_batch = ' '.join([
+        'sbatch',
+        '-p %s'%queue,
+        '--account=t3',
+        '-o %s/logs/chunk%d.log' %(out_dir, ijob),
+        '-e %s/errs/chunk%d.err' %(out_dir, ijob),
+        '--job-name=%d_%s' %(ijob, out_dir),
+        '--time=%d'%time,
+        '--nodes=1 --ntasks=1 --nodelist=t3wn[80-91]',
+        '--mem=4000',
+        '%s/submitter_chunk%d.sh' %(out_dir, ijob),
+    ])
+
+    print(command_sh_batch)
+    if not args.dry_run:
+        os.system(command_sh_batch)
+    n_submitted += 1
+
+print('#### %s %d chunk(s) of %d into %s'
+      % ('wrote (dry run)' if args.dry_run else 'submitted', n_submitted, len(chunks), out_dir))
